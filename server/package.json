{
  "name": "media3xv-server",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "start:cluster": "node server.mjs --cluster",
    "dev": "nodemon --watch . --ignore '../.thumb-cache/**' --ignore '../.temp_livephotos/**' --ignore '../.git/**' --ignore '../node_modules/**' server.mjs"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
