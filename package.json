{
  "name": "media3xv-vault",
  "private": true,
  "scripts": {
    "start": "npm --prefix client run start",
    "dev": "npm --prefix client run dev",
    "start:server": "npm --prefix server run start",
    "dev:server": "npm --prefix server run dev",
    "start:cluster": "npm --prefix server run start:cluster"
  }
}
